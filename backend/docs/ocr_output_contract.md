# OCR Output Contract (Phase 2)

Each page returns:
- words: raw OCR words
- lines: grouped words per line
- blocks: grouped lines
- tables: detected table regions (optional)

This contract is stable for downstream agents.